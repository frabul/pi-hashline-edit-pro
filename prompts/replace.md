Replace a range of lines (or a single line) in a text file, targeted by the 3-char anchors from read_with_anchors output. remove_from and remove_to must each be a BARE 3-character anchor: copy only the anchor from the leftmost column of a read row (row `ve7│function hello() {` means `"remove_from": "ve7"`). Never pass the line content, a code line, or a paragraph into these fields.
