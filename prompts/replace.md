Replace a range of lines (or a single line) in a text file, targeted by 3-character anchors from read_with_anchors output. Example: read_with_anchors showed `aB3│old` and `kQm│old2` — to replace both, use {"remove_from":"aB3","remove_to":"kQm","replacement_lines":["new line 1","new line 2"]}. Single line → use the same anchor for both `remove_from` and `remove_to`.
