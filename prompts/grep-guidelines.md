- `anchor_grep`: to modify a known line without knowing its line number, run `anchor_grep` for that exact snippet (add `context` as needed) and edit the returned `anchor│content` row directly — don't `read_with_anchors` the whole file just to find it.
